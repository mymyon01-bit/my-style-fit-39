// RecommendationFeed is now integrated directly into DiscoverPage.
// This component is kept as a re-export for backward compatibility but is no longer used.
export default function RecommendationFeed() {
  return null;
}
