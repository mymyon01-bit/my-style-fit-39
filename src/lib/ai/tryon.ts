// ─── AI TRY-ON (SAFE MODE) ──────────────────────────────────────────────────
// Stub that never blocks the UI. Real try-on lives in fit-tryon edge fn and
// is invoked separately from the main fit visual flow.

export async function generateTryOn(): Promise<string | null> {
  return null;
}
